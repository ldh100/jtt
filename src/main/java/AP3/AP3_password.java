/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package AP3;
import A.TWeb;
import static A.A.*;
import static AP3.AP3.SiteID;
import static AP3.AP3.appId;
import static AP3.AP3.url;
/**
 *
 * @author Oleg.Spozito
 */
public class AP3_password {
    public static void run() throws InterruptedException { 
        _t++; Thread.sleep((long) sleep); TWeb.Navigate_to_URL("Navigate to Site -> Brand List", url + "#/sites/" + appId + "/site/" + SiteID, "no_jira");
        if (FAIL) { return;}
        // find text  ddddd
        //ck=lick on this elemnt
    }  
}
